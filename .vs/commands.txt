git push -u origin main

git commit -am "commit message"

git commit -m "Title" -m "Description .........."

git init -b main

git add -p

//This is nothing, just saving some commands in GitHub because I'm new.